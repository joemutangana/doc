package bank;

public class Notes {

    /*
     * 
     * Object in java:
     * What is an object?
     * 
     * 204 room contain 100 people, and each person in 204 has some information if
     * we compare thiose info we can have same info that differ all 100 people EX:
     * id,name, age
     * 
     * If we have a group of people they form what we call a class
     * an individual person is considered and object.
     * 
     * Cars: 100 cars 1 car among them is an object
     * 
     * nythingis an obejct, therefor an object is a set of infromartion and it's
     * action/methods.
     * 
     * -----------------------------------------------------------------------------
     * --
     * 
     * State of anobject/atrributes: i s aprocess of identifying te set of variables
     * that represents an object.
     * 
     * An object is derived froma class
     * 
     * 
     * 
     * 
     * 
     * HOW DO WE CREATE AN OBJECT?
     * -------------------------------
     * 
     * int age;
     * 
     * The int is a class and age is an object.
     * 
     * to initialize the object we use constructor
     * 
     * 
     * A constrauctor is a kind of method (), but it has a class name.
     * 
     * Saving: Class, Saving()
     * 
     * A constructure is created at the time when u create a class, but it diesn't
     * apper in a class instead is is kept under java virtual machine(jvm).
     * 
     * Constructor is there to initilise the object.
     * 
     * 
     * 
     * 
     */
    
}
